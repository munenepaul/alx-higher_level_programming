#!/usr/bin/python3
def multiply_by_2(another_dictionary):
    """Returns a new dictionary with all values multiplied by 2"""
    multiplied_dict = key: (another_dictionary[key] * 2)
    for key in another_dictionary
    return multiplied_dict
